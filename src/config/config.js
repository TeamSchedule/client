const DOMAIN = "localhost";
const ENABLE_HTTPS = false;

export const SERVER_ORIGIN = `http${ENABLE_HTTPS ? "s" : ""}://${DOMAIN}/`;
export const AVATARS_STATIC_SERVER = 'http://localhost:81/avatars';
