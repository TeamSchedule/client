// REQUESTS CONFIG ======================================================
const DOMAIN_URL = "localhost:8080";
const ENABLE_HTTPS = false;
export const BASE_URL = `http${ENABLE_HTTPS ? "s" : ""}://${DOMAIN_URL}/`;
